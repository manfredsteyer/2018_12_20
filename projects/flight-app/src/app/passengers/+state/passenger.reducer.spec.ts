import { reducer, initialState } from './passengers\+state\passenger.reducer';

describe('Passengers\+state\Passenger Reducer', () => {
  describe('unknown action', () => {
    it('should return the initial state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
